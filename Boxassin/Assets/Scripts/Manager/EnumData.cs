using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class EnumData
{
    public enum TileType { Road, Obstacle, Gimmick, PlayerGen, GuardGen, TargetGen }
    public enum GeneratorType { Player, Guard, Target }
}
